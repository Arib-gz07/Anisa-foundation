# Optional. The public Turnstile site key is already in src/config/site.ts,
# so Vercel deploys work with no env setup.
# PUBLIC_TURNSTILE_SITE_KEY=

# Private secret — only needed later when form email is verified on a server.
# Never commit real values.
# TURNSTILE_SECRET_KEY=
